exibirAsteriscos(10);

function exibirAsteriscos(linhas) {
    for(let linha = 1; linhas <= linhas; linha++){
        let padrao = '';
        for (let i = 0; i < linha; i++)
            padrao += '*';
    }
        console.log(padrao);
}

